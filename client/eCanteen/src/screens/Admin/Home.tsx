import { View, Text, SafeAreaView } from 'react-native';
import React from 'react';

const Home = () => {
    return (
        <SafeAreaView>
            <Text>THIS IS ADMIN HOME PAGE</Text>
        </SafeAreaView>
    );
};

export default Home;
