import { SafeAreaView, Text } from 'react-native';
import React from 'react';

const Orders = () => {
    return (
        <SafeAreaView>
            <Text>This is admin orders page</Text>
        </SafeAreaView>
    );
};

export default Orders;
