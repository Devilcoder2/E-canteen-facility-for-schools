import { SafeAreaView, Text } from 'react-native';
import React from 'react';

const NewItem = () => {
    return (
        <SafeAreaView>
            <Text>NewItem</Text>
        </SafeAreaView>
    );
};

export default NewItem;
